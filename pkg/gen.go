// package swagger

// //go:generate rm -rf api/models
// //go:generate rm -rf api/restapi
// //go:generate swagger generate server --quiet --target api --name social-naka-app --spec swagger.yml --exclude-main --template=stratoscale
